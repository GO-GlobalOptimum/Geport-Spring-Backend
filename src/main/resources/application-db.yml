spring:
  datasource:
    url: jdbc:mysql://geport.mysql.database.azure.com/geport_test
    username: "geport"
    password: "!!aA04180303a!!"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
